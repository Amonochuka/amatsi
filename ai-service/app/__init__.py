# ============================================================================
# ai-service app package marker (intentionally empty).
# Kept for Python package imports (app.*, services.*, routes.*).
# No implementation required.
# ============================================================================